import styled from '@emotion/styled'
import { space, layout, flexbox, boxShadow, border, color } from 'styled-system'

export const Box = styled('div')(
    space,
    layout,
    flexbox,
    boxShadow,
    border,
    color,
);